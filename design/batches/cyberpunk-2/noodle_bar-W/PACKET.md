# noodle_bar/W — technique t2 (labelled scaffold)

> **This ask WAITS for `noodle_bar/E`. Do not paint it until that wall's picture is on disk.**
>
> [row 42] Every location paints one wall first — its LEAD, the most-carried wall — and the other three are painted with the lead's own picture in front of the painter, which is what makes the four read as one room. This facing continues `noodle_bar/E`, which has no picture yet.

There is NO Image 1 in this packet and none is to be found elsewhere — the medium is in the prompt's own words. Attach `scaffold.png` as **Image 1**, in that
order, then send `prompt.txt` verbatim. Generate 1 images and save them to the
exact paths below — the measurement runs the moment a file appears at one of them.

| roll 1 | `backdrops/source/noodle_bar-W/row23-70f45eab.png` |

The prompt files are already on disk beside them. Do not rewrite them.

This wall: 227.6 px per metre at the wall plane, no carrier — unbroken corrugated galvanised sheet below the cable channel with stained bare concrete block above it.
Voice: **noodle_counter_trade** (room id); gate anchor **the surface-mounted cable channel above the floor**, 1.10 m.
Register: **g5-noappendix** — the register this ask was composed in (tools/frame-language.mjs, row 43). Every
roll below is attributable to it: the reading of a return joins to this line through the roll id.
Write only under `backdrops/`. Never `src/`, never `design/`.
