# noodle_bar/S — RE-ASK 2 (technique t2, labelled scaffold)

> **Why this wall is being asked again**
>
> the glazing: this window's glass is wired safety glass in a steel frame, grimed and faintly green — flat panes with a fine wire mesh inside the glass; NO diamond quarries, NO leaded lattice, no cames, no coloured glass; the previous attempt painted seventeenth-century leaded diamond panes, which do not exist in this world

**Image 1 is a DERIVED style seed, not a wall.** `style-noodle_bar.png` is `backdrops/noodle_bar/E.png` — noodle_bar/E, this room's own wall (sha256 `f11acfc73e7f`) — with every opening and carrier on it filled in from that wall's OWN adjacent fabric by `tools/style-seed.mjs`: 1 rectangle(s), 32.13 % of the wall, the floor and the ceiling untouched. The fill report rides beside it as `style-noodle_bar.json` and the store's copy is `backdrops/style-seeds/noodle_bar-E.png` (sha256 `7164a1cb4353`). every opening that was in this painting is gone from the seed: 1 way(s) through and 0 glazed opening(s) went in, and NOTHING the detectors read in the result stands anywhere the fill touched; the count did not rise, so nothing was manufactured either So it carries this room's materials, its palette and its light and NO ARCHITECTURE AT ALL: how many openings the wall being painted carries, where they stand and every dimension of them come from the layout image and the words.

**Image 3 is this wall's edge seed.** `edge-seed-left.png` is the 10 % of `backdrops/noodle_bar/E.png` that abuts this picture — its right-hand 154 columns, full frame height, cut by `tools/crop-edge-seed.py` (sha256 `d9df17bdcc71` from a painting at `f11acfc73e7f`). The prompt names its role in words: _Image 3 is a reference of exactly what sits at this picture's left edge - the scene continues from it seamlessly._

Seeding here is **opportunistic** — an indoor location: the strip anchors material tone and the wainscot line across the corner (promoted).

Attach `style-noodle_bar.png` as **Image 1** (noodle_bar/E, this room's own wall with its openings removed), `scaffold.png` as **Image 2** and `edge-seed-left.png` as **Image 3**, in that
order, then send `prompt.txt` verbatim. Generate 1 images and save them to the
exact paths below — the measurement runs the moment a file appears at one of them.

| roll 1 | `backdrops/source/noodle_bar-S/row23-4106c388.png` |

The prompt files are already on disk beside them. Do not rewrite them.

This wall: 243.8 px per metre at the wall plane, window.
Voice: **noodle_counter_trade** (room id); gate anchor **the surface-mounted cable channel above the floor**, 1.10 m.
Register: **g5-noappendix** — the register this ask was composed in (tools/frame-language.mjs, row 43). Every
roll below is attributable to it: the reading of a return joins to this line through the roll id.
The earlier ask for this wall is still at `../` and is not overwritten.
Write only under `backdrops/`. Never `src/`, never `design/`.
