# Packet — master_bedchamber/N, arm s2-ink-sheet (INK ON PAPER)

the same ask with the layout image cut as a line drawing on paper: thin ink where surfaces meet, carrier boxes as outlines with a hatched interior, no tone anywhere a painter could read as a material or an opening - and the register's one sentence saying that is what it is

**Generate 1 image. Save it to the exact path in the table.** The measurement runs the
moment a file appears at one of those paths, so a return in the right place under the wrong name
costs a roll.

## Attach, in this order

1. `scaffold.png` — **Image 1** — the annotated layout scaffold, cut as a line drawing in ink on paper

**There is no style image, and that is not an omission.** [HUMAN, 2026-08-24] "So why do we give it the reference image of the study? I think it biases it too much." Image 1 is never a wall from another room, and although the ruling would allow **master_bedchamber/E** here, this arm declares no Image 1 — the layout picture is the whole of what this ask is shown, and the medium is in the prompt's own words. Attach the one image above as **Image 1**.

Then send the prompt text verbatim from `prompt.txt`.

## The rolls

| roll | save the image to |
|---|---|
| 1 | `backdrops/source/master_bedchamber-N/scafink-5c99cb38.png` |

**The prompt file is already on disk beside where each image goes** (`backdrops/source/master_bedchamber-N/scafink-5c99cb38.prompt.txt`), written
from this packet, so a return needs nothing but the PNG. Do not rewrite them.

## What this wall is

master_bedchamber/N, generation 1. 143.2 px per metre at the wall
plane; fireplace + door.
Voice **bedchamber**; gate anchor **the wainscot chair-rail above the floor**, 0.95 m.

## The fence

Write only under `backdrops/`. Never `src/`, never `design/`. Nothing here asks you to judge a
result: generate, save to the named paths, and report the paths back.
