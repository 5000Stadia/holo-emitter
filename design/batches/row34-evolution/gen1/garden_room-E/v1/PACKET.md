# Packet — garden_room/E, arm v1 (TEXT-ONLY-PRECISION)

no layout image at all; the full geometry as exhaustive precise text

**Generate 2 images. Save each to the exact path in the table.** The measurement runs the
moment a file appears at one of those paths, so a return in the right place under the wrong name
costs a roll.

## Attach, in this order

1. `style-seed-warm.png` — **Image 1** — the style reference, Kabe's approved seed ("Warm", `design/approvals.log`, 2026-08-21)

**There is no second image, and that is this arm.** The whole geometry is in the prompt.

Then send the prompt text verbatim from `prompt.txt`.

## The rolls

| roll | save the image to |
|---|---|
| 1 | `backdrops/source/garden_room-E/row34-479d9fa0.png` |
| 2 | `backdrops/source/garden_room-E/row34-4eba6112.png` |

**The prompt file is already on disk beside where each image goes** (`backdrops/source/garden_room-E/row34-479d9fa0.prompt.txt`), written
from this packet, so a return needs nothing but the PNG. Do not rewrite them.

## What this wall is

garden_room/E, generation 1. 155.2 px per metre at the wall
plane; door.
Voice **garden_parlour**; gate anchor **the wainscot chair-rail above the floor**, 0.95 m.

## The fence

Write only under `backdrops/`. Never `src/`, never `design/`. Nothing here asks you to judge a
result: generate, save to the named paths, and report the paths back.
