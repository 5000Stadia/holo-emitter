# gallery/W — technique t2 (labelled scaffold)

**Image 2 is this wall's edge seed.** `edge-seed-right.png` is the 10 % of `backdrops/source/gallery-N/row23-f6591bea.png` that abuts this picture — its left-hand 154 columns, full frame height, cut by `tools/crop-edge-seed.py` (sha256 `94be200172cd` from a painting at `3f75defa3d78`). The prompt names its role in words: _Image 2 is a reference of exactly what sits at this picture's right edge - the scene continues from it seamlessly._

Seeding here is **opportunistic** — an indoor location: the strip anchors material tone and the wainscot line across the corner (candidate).

There is NO Image 1 in this packet and none is to be found elsewhere — the medium is in the prompt's own words. Attach `scaffold.png` as **Image 1** and `edge-seed-right.png` as **Image 2**, in that
order, then send `prompt.txt` verbatim. Generate 2 images and save them to the
exact paths below — the measurement runs the moment a file appears at one of them.

| roll 1 | `backdrops/source/gallery-W/row23-d5da099b.png` |
| roll 2 | `backdrops/source/gallery-W/row23-33671a10.png` |

The prompt files are already on disk beside them. Do not rewrite them.

This wall: 213.3 px per metre at the wall plane, no carrier — unbroken warm figured walnut veneer above the dado's chrome capping strip with dark macassar-ebony veneer below.
Situations: enclosed, follower, no-window, blank (see `design/playbook-facings.md`).
Voice: **gallery_walnut** (room id); gate anchor **the dado's chrome capping strip above the floor**, 1.20 m.
Register: **g5-noappendix** — the register this ask was composed in (tools/frame-language.mjs, row 43). Every
roll below is attributable to it: the reading of a return joins to this line through the roll id.
Write only under `backdrops/`. Never `src/`, never `design/`.
