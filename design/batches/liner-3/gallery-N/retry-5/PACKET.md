# gallery/N — RE-ASK 5 (technique t2, labelled scaffold)

> **Why this wall is being asked again**
>
> Image 1 is already drawn at the ruled scale for this camera; return the same picture at exactly the same size - the wall must not come back smaller, farther away, or with more ceiling and floor around it than Image 1 shows

**Image 1 IS THE GROW DRAFT** — `grown-gallery-N.png`: the room's own 1x1 painting with its back wall cut out and refitted at the far box (one uniform shrink), the shell kept at its own painted angles, and the middle left as a wireframe ring for the painter to fill [Kabe's grow step, 2026-08-31].

Attach `grown-gallery-N.png` as **Image 1** (the room part-painted: near half and far wall finished, the middle a wireframe ring to paint) and `scaffold.png` as **Image 2**, in that
order, then send `prompt.txt` verbatim. Generate 2 images and save them to the
exact paths below — the measurement runs the moment a file appears at one of them.

| roll 1 | `backdrops/source/gallery-N/row23-a9024bfc.png` |
| roll 2 | `backdrops/source/gallery-N/row23-079f9a01.png` |

The prompt files are already on disk beside them. Do not rewrite them.

This wall: 213.3 px per metre at the wall plane, door.
Situations: enclosed, lead, run-wall:corner-left, door, no-window (see `design/playbook-facings.md`).
Voice: **gallery_walnut** (room id); gate anchor **the dado's chrome capping strip above the floor**, 1.20 m.
Register: **g5-noappendix** — the register this ask was composed in (tools/frame-language.mjs, row 43). Every
roll below is attributable to it: the reading of a return joins to this line through the roll id.
The earlier ask for this wall is still at `../` and is not overwritten.
Write only under `backdrops/`. Never `src/`, never `design/`.
