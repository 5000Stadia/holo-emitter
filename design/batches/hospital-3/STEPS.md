# hospital-3 — step log (2026-08-30)

Rule for this log: every step, its wall-clock, its outcome, and — for anything that failed or hung — the cause and the fix that makes it not happen again. [Kabe, 2026-08-30] "check that as it deploys there are no hang ups or failed steps. If there are, do a bit of a report on them and collaborate how to execute cleanly on each step."

| # | step | time | outcome | hang-up / cause | fix (now in the tree) |
|---|---|---|---|---|---|
| 0 | author packs/hospital-3 (plan, pack, voices, world) from the cyberpunk template; standpoints by the validator's own law (K=0.25; threshold rule where a wall would not fit the lens) | ~1 Navigator turn | plan valid first try | — | — |
| 1 | derive-world --pack hospital-3 | <1 s | 3 locations, 4 exits, 12 lines | — | — |
| 2 | validate-plan --pack hospital-3 | <1 s | **validated the manor's plan** against the hospital's archetypes (22 findings) | `--pack` chose the voices, the default fixture dir stayed demo-study | validate-plan and validate-fixtures: `--pack` names the fixture dir |
| 2b | validate-fixtures --pack hospital-3 | <1 s | 1 finding: two doors into the treatment room shared one refusal line | derive-world's template named only the destination | the line names the door's own side too |
| 3 | make-scaffold --emit-manor --pack hospital-3 | 31 s → error | **30 s silent timeout**: the page never booted because fixtures/hospital-3/fixture.js was not baked | bake-fixtures had no `--pack` and the emitter waited on the page with no check | bake-fixtures takes `--pack`; the emitter refuses by name before launching a browser |
| 3b | bake-fixtures --pack hospital-3 | <1 s (after two Navigator slips: an unimported `resolve`, then a doubled absolute path) | fixture.js 15 kB | pack paths are already absolute | — |
| 3c | emit, first read of a prompt | 2 s | 12/12 packets; **leak**: the window sentence asked a 1995 hospital for "a plain chamfered stone surround… two stone mullions… a casement hung on iron hinges", head 2.00 m against the pack's 2.10 | window words lived in room-voices.mjs / frame-language.mjs; the scaffold's sill/head were literals in make-scaffold | `conventions.window` in every pack's world.json (surround by rank, mullion, transom, opening light); sill/head read from the pack; the register reads them through ctx |
| 3d | re-emit | 2 s | 12/12, prompt reads: steel frame, transom bars, top-hung vent, head 2.10 | — | — |
| 4 | dispatch the painter (AgentPost → holoemitter-assets) | — | **QUEUED, not delivered**: "no live mailbox consumer; Codex observed stale hooks (0.0.6 vs installed 0.0.8)" — the seat would never have woken | the seat's Codex session runs hooks from a stale cache | typed the order into the seat's tmux pane (`tmux send-keys`); it began work in 7 s. Clean fix owed: reinstall the AgentPost Codex hooks for the seat (`agentpost install codex --agent holoemitter-assets --project .`) and reload Codex, or make the pane-typed order the standard dispatch (it is deterministic and needs no adapter) |
| 5 | start the loop for the pack (`HOLO_PACK=hospital-3 row23_run.py --watch` in tmux `hospital-loop`) | <1 s | up | the standing manor loop only ever serves one pack; a second pack needs its own session — `tools/services.sh` should take the pack as a parameter | owed |
| — | prompt-guard specs (material-origin, room-voices, scaffold, voice) | 40 s | room-voices + scaffold green; 4 material-origin + 8 voice cases red — **identically red on the previous commit** (pre-existing) | the suite is not green at HEAD; nobody noticed because merges run it in a worktree and the last merge's numbers were not re-checked | owed: a red-at-HEAD report and a fix pass before the next merge |
| 6 | paint — the three leads (reception/N, treatment_room/E, ward/S) | 8 min from the typed order to three files on disk | 3/3 on brief at a look: handrail at the ruled height, sheet below / paint above, steel windows with the top-hung vent open, coved vinyl, no lettering | — | — |
| 7 | measure → warp → promote, the three leads | the loop took them within its next pass | **3/3 promoted, 0 re-asks** (reception/N and treatment_room/E through the warp, camera PASS; ward/S through the measured camera, no warp needed) | instruments recorded quietly: (a) the window detector calls BOTH painted windows `unpainted` (a 1660-mullion detector on a 1995 steel window — recorded not gated, so nothing was refused); (b) ward/S's promotion carries `measured_round: "manor"` — a theme word as a round label; (c) reception/N's floor line was read at source row 759 where the painted base sits nearer 735 (the skirting-shadow rule again, see B-FLOOR) | owed: window detector on the pack's window words (or the trace's rectangle prior) — B-FLOOR covers (c); the label is a rename |
| 7b | (cause of 7(b)) | — | `--round manor` is a READINGS DIRECTORY, not a label: `design/plan-draft/measured/manor/<loc>-<F>.json` for every pack; promote's regex forbids a hyphen, so `hospital-3` cannot even be passed; each pack's `paths.readings_dir` is declared and unused | the round predates packs | owed: measure.py and promote-backdrop file readings under the pack's `readings_dir`; the loop passes it; the manor keeps its directory |
| B-FLOOR | theme-neutral floor-line reader (capped builder, branch `floor-line-witnesses` @ ba0a323) | 29 min, 155k tokens | **capped, not merged**: `floor_line.py` with corner / step / declared-legacy witnesses + refusal, 14 unit tests green (unittest — the repo has no pytest); step witness reads back_office/W at 773 (true ~770); but the corner witness fires spuriously at the top of its span (694/706) and outvotes it → 700, and the manor regression is 4/61 within ±6 px with 29 refusals; NOT wired into row23_lib / mesh_warp | (1) the corner witness needs a longer-memory verticality test or must refuse when its answer sits at the top of its span; (2) on manor walls the strongest step is the skirting CAP, ~34 px above the seam — the manor's declared rule must stay the manor's floor line, and the step witness has to reproduce it before anything is wired | second capped builder on the branch, after this run: fix (1), gate on a manor regression ≥ 55/61, wire through `_floor_and_rail` with `measurement_withheld` on refusal, re-warp back_office/W |
| 8 | paint, the nine followers | 3 / 5 / 6 min after the order (reception, treatment room, ward) — **12/12 on disk in 6 min**; the painter reported `unresolved=0` | all on brief at a look; followers hold their lead's materials and rail height | — | — |
| 9 | measure → warp → promote, all twelve | 14 min order-to-last-promotion | **12/12 promoted, 1 attempt each, 0 re-asks** (5 through the warp on the declared camera, 7 on the measured camera) | the loop's stdout is block-buffered to its log (0 lines while it worked) — progress was read off run-state.json | owed: `python3 -u` in services.sh |
| 10 | validate-fixtures, publish | 36 s (two publishes) | valid; **publish refused once**: "fixture.js was stale against the promoted metas; re-baked now — commit and publish again", then live @ df6d132 | the hospital loop promotes but does not re-bake the pack's fixture (the sweep's bake door is the manor's); the publish preflight caught it | owed: the loop bakes the pack it promoted (`bake-fixtures --pack`) after every promotion, so publish is one command |
| 11 | the look: reception/E through to the treatment room (design/batches/throughview/hospital-reception-E-after.png) | 10 s | the far room ends at the threshold with a sill at the foot; its own far door shows dark (no recursion) and its rail continues | one look item: the top ~40 px of the warped wall is streaked — the warp's reveal fill at the ceiling edge when the ceiling line is pinned above the frame (target −24 px) | owed: the fill should not smear a row that lies outside the frame; clamp the ceiling pin at row 0 or paint the strip from the far frame's own band |

**Totals.** 12 walls: 12 images (1.0 images/wall, as the cyberpunk proof), 0 re-asks, 0 holds; 24 min from "author the pack" to live. Live: https://5000stadia.github.io/holo-emitter/?world=hospital-3

**Hang-ups, ranked by what they cost:** (1) the theme leaking into the window sentence (would have painted 1660 windows into every hospital wall — caught by reading the first prompt; fixed as pack data); (2) `--pack` not naming the fixture dir in three tools (validated the wrong plan, then a 30 s silent timeout; fixed); (3) the painter unreachable by mail (hooks stale; typed into its pane; reinstall owed); (4) the round/readings directory shared by every pack (owed); (5) publish needs two runs because the loop does not re-bake the pack it promoted (owed); (6) the suite is red at HEAD before any of today's work (owed). Instruments recording quietly: the window detector calls every painted hospital window unpainted; the floor-line rule is the manor's (B-FLOOR capped, reader built, not wired).
