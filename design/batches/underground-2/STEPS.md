# underground-2 — step log (2026-08-30)

[Kabe]: a new two-room test, genre the Navigator's pick, unique to test genre-specific things; room two "two room squares wide", walked with an up arrow, not a turn; all rooms counted in small room boxes. Genre picked: a 1931 London Underground station, BRUNSWICK HALT — glazed tiling, a dado band ruler, no windows below ground (the first pack to stress the no-window clause everywhere), and a hand-tinted-photograph medium.

| # | step | time | outcome | hang-up / cause | fix (now in the tree) |
|---|---|---|---|---|---|
| 0 | author packs/underground-2: booking hall (1 box) + PLATFORM (2 boxes: cells `platform` + `platform_far`, one name, joined by a full-width `open_edge`); box unit 6.4 m recorded in pack.json | ~1 turn | plan authored | — | — |
| 1 | the long-room law | — | validator refused: "wall_line 19.8 is not the line this facing views (13.4)" — a facing had to view its own edge | the deep facings look ACROSS the open edge to the far cell's wall (two paintings of one wall at 11.2 m and 4.8 m; the up arrow cuts between them) | validate-plan: a facing may view the first RULED line across its own full-width open edges (`throughLine`, 8-hop cap); a narrower mouth still views its edge (the court) |
| 2 | the up arrow | — | built | the page had no walk-forward control (thresholds were click-hunting) | `#chevron-forward` (chrome, bottom-centre, ↑ / ArrowUp): shown when the facing carries a `way_` exit (derive-world's name for an open_edge); dispatches the same go; keyboard.spec pins the new focusable |
| 3 | same-name narration | — | built | "You pass from the platform into the platform." | derive-world: walking within one name reads "You walk on down / back along the platform." (directed by the exit's facing so the two ways differ — the validator refuses shared prose) |
| 4 | derive → validate → bake → emit | ~40 s | 12/12 packets (3 leads), prompts leak-clean; the deep ask reads "the whole 6.4 m of the wall spans about half the picture's width" (11.2 m camera — the long-platform shot) | whereami.spec's surface census was missing every non-manor place (red since cyberpunk, unnoticed in the suite's standing red) | the 8 places enumerated in design/surface-strings.md; spec green |
| 5 | paint + measure, first pass | leads at 2 min, all 12 by 6 min | booking_hall/E promoted (PASS −5.9%), platform/W promoted (warped); **platform_far/W HELD**: "every column band this scaffold declares falls outside the frame" | the crossed open edge registered as the deep facing's CARRIER: projected nearer than the viewed wall it spanned the whole frame, swallowed every column band, and the ask read "there is no wall here at all ... flanked by low coursed-stone piers" (the manor court's own language) — which the painter faithfully painted (dark voids and stone piers beside a floating wall) | `facingCarriers`: an open edge is a carrier only when the facing views its own edge; a deep facing takes the viewed wall's carriers. Both deep asks re-emitted clean (carriers ['door'], two column bands); the two polluted candidates discarded; 2 re-asks sent (the run's first) |
| 5b | (observed in the same pass) | — | one transient "end-of-sweep bake refused: cePromise.__proto__ (node ESM loader)" — the next pass baked clean | unexplained one-off in the loop's bake subprocess | watched; recurs → its own row |
| 5c | two loops baked the shared served tree at once and the fixed `served.building` name made them delete each other's half-encoded work | — | **fixed**: per-process build dirs (`served.building-<pid>`), stale ones swept after 15 min; both swaps land whole trees, last writer wins | my atomic-swap fix assumed one baker | — |
| 6 | the two clean deep paintings held AGAIN on the old reason | 15 min to root | the re-emit reuses deterministic roll ids; the painter delivered NEW paintings at the SAME paths; the reading cache (id + instrument) served the OLD paintings' readings for the new bytes | "a reading is a pure function of its bytes and the instrument" — the id stood in for the bytes and the bytes moved under it | readings and the routing latch carry `candidate_sha256`; a repainted candidate at the same path is measured and routed afresh; the two stale readings deleted, loop restarted |
| 7 | the warp carried platform_far/W and the PROMOTION refused it: door01 and way01 "share 110 px — one hole cannot be two ways through" | 15:51 | **fixed, promoted 15:55 (warped)** | the crossed-edge law lived in `facingCarriers` only; its sibling `openingsForFacing` still wrote the full-width open edge into the deep wall's meta as a threshold | the same law in the threshold block; the JS projection/promotion files joined the instrument digest so a fix there re-routes latched walls by itself |
| 7b | my latch patch crashed the loop (NameError: `reading` out of scope in `_record_exit`) | 15:48 | fixed (parameter, not a free variable) | patched live and written to disk before its syntax parse ran | the causes note (E): parse before write, one gesture for reset-and-restart |
| 8 | [Kabe] "determine what will prevent rerolls — assume we did it bad; better prompting, and better is not more" | 90 min | **answered with an experiment** (design/audit/reroll-research-2026-08-30.md): the 3 far-cell rerolls — seeded rolls FAIL −13/−12/−9 %, unseeded re-rolls with the SAME words PASS +0.4/−3.5/−7.4 — the wrong Image 1 caused them, not the words | one attached image out-argues every sentence (row 40's shields, now scale) | a seed must PASS the instrument before it may teach (styleImageFor gate); the warp is reachable for scale misses (one reading threaded in — its own snap re-read had crashed on shape); a partial emit no longer guts the manifest's skipped entries (the sweep could not see the walls or their retry rolls) |

**Totals.** 12 walls, 15 images (1.25/wall — the three rerolls are the research subject of step 8 and paid for the seed-gate law), 12/12 promoted (8 warped, 4 measured); the up arrow walks the two-box platform both ways. Live: https://5000stadia.github.io/holo-emitter/?world=underground-2

**What this genre bought the engine:** the long-room laws (a facing views the first ruled line across its full-width open edges; a crossed edge is neither carrier nor threshold of the viewed wall), the up-arrow control, same-name narration, the seed-gate law (a picture must pass the instrument before it may teach), one reading threaded into the warp, byte-keyed reading identity, skip-preserving manifests, per-process bakes — and the no-window clause held on all 12 walls.
| 9 | [Kabe] the long-room views don't match in detail ("geometrically sound as a room but not as details"); his mechanism: "a separate image request made proper to the image gen, acknowledging FROM the close up that this is based off that picture but X feet backwards" | ~60 min | **built, two walls re-asked** | two independent asks painted one wall twice (disc grew a face, one lamp became three): nothing carried the shared wall's CONTENT between standpoints, and the register's Image-1 clause ("take no architecture from it") is the opposite of what a deep view needs | `sameWallImageFor` in the one home (`styleImageFor`): a deep facing's Image 1 is the promoted CLOSE painting, raw, with the identity sentence and the camera move in metres; the register and PACKET.md say exactly that; gated on promotion (a seed must pass the instrument). Three of my own slips on the way (a third style call site, an unimported `relative`, template text left speaking derived-seed language) — each caught by reading the emitted packet before ordering paint. A deterministic derived-view assembler (B-DEEPVIEW, capped builder) runs in parallel as the comparison |
| 10 | [Kabe] "facing the long wall, that edge looks like our previous small-room corner but should extend the flat wall off screen" | ~50 min | **built, four side walls re-asked** | each cell's side facing was asked as a cosy two-corner room; in a long room the side wall is one 12.8 m RUN — no corner stands at a crossed edge | `runSpanOf` (plan-projection): a side wall's corners extend across full-width open edges to the run's real ends, off-frame on the open side (a state the instruments already tolerate); the register says "LONGER than the picture ... on the right there is NO corner and NO return wall: the flat wall simply continues" and gives the in-view metres; the sheet skips junctions whose endpoints the geometry cannot draw. On the way: my runSpanOf argument slip crashed deriveMeta (caught by the verify probe), the sheet's junction NPE, wall_run_m registered in the meta vocabulary — and two painter orders raced the re-emits (cause E), so four stale rolls were painted and discarded before the clean order |
| 11 | the four run-wall paintings (one corner, wall off frame — as asked) are refused by the instruments | found 17:5x | **the ask outran the instruments**: the horizon fit wants TWO converging returns; the warp's landmark reader wants TWO corners → `meshwarp.landmark_unreadable` → a re-ask that would repaint the same correct picture (a reroll loop) | the run-wall situation reached the register and the plan but not the measuring half | builder dispatched: single-corner landmark reading (x-scale from the ruled anchor, translation from the one in-frame corner), one-return horizon acceptance routing to the warp; no re-asks spent meanwhile |
| 12 | [Kabe] the playbook: instruction sets per direction and room-piece type, composed as overlays, skipped when absent | builder, 11 min | **landed**: `situationsOf` (17 tags), per-pack report at schematic time, packets/manifest stamped, `design/playbook-facings.md`; found and defused a latent crash (deepViewOf on open facings) | — | next: the register reads one instruction block per tag |
| 13 | run-wall instruments (builder) | 15 min build | **merged**: `run_wall`/`run_source_corners` (one in-frame corner + the ruled anchor give the frame; the open side is a `run_end`, never a pin), the warp's target clamps the open end at the frame edge; all four refused walls warp with 0.0 px residuals (reveals 0.07–2.75 %); the warp's own readings cache was pack-blind (`measured/manor` hardcoded) and now reads the pack's — the "readings dir shared" owed item, closed by the way | — | follow-up owed: the horizon fit still lands run walls as holds (correct route, but they never pass on their own terms — one-ramp fitting is the next instrument row); the single corner's `corner_ask_px` is recorded unguarded |
| 14 | the deep-wall decision | — | the loop re-promoted the PAINTED same-wall frames; the derived frames were never measured — deep_view drops candidates outside every roll list, so the sweep cannot see them (my wiring assumption, not the builder's fault) | a candidate is only what a roll record names | owed IF Kabe picks derived: register derived frames as rolls (the retries mechanism takes fresh opaque ids); the choice sheet is with Kabe |
| 15 | [Kabe's mechanism, verbatim in tools/deep-draft.py] the deep view: "a reference image that is the close-up shrunk down to the correct proportion ... then image generation requesting the image be recreated to eliminate the weirdness of the empty space or the skewed filler" | ~40 min | **built, two walls re-asked**: `tools/deep-draft.py` (uniform k from the two metas' ppm — a circle stays a circle; margins = edge pixels stretched, honestly filler), wired through attachStyle's same-wall branch; the register asks for the picture RECREATED — centre kept exactly, filler replaced with the room continuing | the painted+warp route pinned width and height independently (the vertical-oval disc) and smeared the reveals; the derived assembly was exact but collaged; Kabe's draft+recreate takes the geometry from the machine and the paint from the painter | — |
| 15b | the draft-recreate outcome | — | **live @ 6a9cfd7**: painted margins (no smear, no collage), details identical to the close view; residual: the painter recreated ~33 % larger in x / ~12 % in y than the true-scale draft, and the warp's independent x/y pinning left the disc a ~16 % oval | the recreation drifts even from a true-scale reference — an image model fills its frame; the warp then re-imposes geometry per-axis | offered to Kabe: an isotropic warp mode for draft candidates (one uniform scale from the floor/ceiling rows, centre-column translation, corners recorded off-pin; traced doors keep click-truth) — circles exactly round at the cost of ~16 % wall-width drift |
| 15c | [Kabe] "consider what is creating the skewed oval" | measured, three stages | **the chain, in ring pixels**: DRAFT 51x53 (round, 0.96) → RECREATION 84x88 (still round, 0.955 — but the whole scene enlarged ~1.6x, MORE in x than y) → WARPED 63x78 (0.81, the oval; 63/84 = 0.750 and 78/88 = 0.886 — exactly the warp's per-axis scales) | the painter re-COMPOSES rather than transforms: it frames the wall wider than tall but draws objects at their natural shape; the warp then restores declared geometry per axis, and that differential squeeze is what ovals a round-drawn disc | the shape-true fix follows: a uniform (isotropic) restore for draft-born candidates preserves object shapes and lets the wall's width drift instead — awaiting Kabe's word |
| 16 | [Kabe] the interleaved-blend mechanism: uniform shrink + evenly-distributed blended-line insertion to meet any stretch ("fuzzy but reference valid without skew") | ~45 min | built (`deep-draft.py --correct`, pins-based per warp band) and **proven faithful**: the repaint-clean ask copied the reference disc 63x74 → 63x75, stutter gone | the transfer primitive works — a painter WILL repaint a reference exactly when asked to | corrections only ever touch references; the repaint-clean ask is a new tool |
| 17 | [Kabe] "doesn't my approach also skew a circle? It does" | minutes — the measurement was already in | **corrected-previous retired**: insertion moves a shape's ENVELOPE as surely as resampling; only the texture of the skew differs. And the previous painting was self-inconsistent (room 33% wide, objects round) — no whole-image mapping can pin its walls and keep its circles | the oval is born wherever an anisotropic mapping touches a picture that holds round-drawn objects | L-ENVELOPE: never map a shape-bearing reference anisotropically — not by resample, not by insertion |
| 18 | [Kabe] the frame recipe: "shrink maintaining aspect ratio, simulate the geometry to the edges, cut off the corner edges, overlay the correct corner geometry as reference lines" | ~40 min | built (`deep-draft.py --frame`: content at ONE uniform scale, wrong-camera junctions cut, declared geometry as ink guide lines; drafts' discs 0.984 both) — four rolls painted, **all four beautiful complete rooms, discs 0.96–0.99 round** | but every roll painted the wall WIDER than the guide lines (800/735 px vs 585 declared): the painter's zoom-in bias ignores thin ink; camera PASSes (material scale right), span misses, the warp squeezes, the disc pays (live 0.75–0.81) | content and geometry never mixed in one mapping; span is the one remaining miss |
| 18b | the sweep promoted a warp-exit roll while an unread camera-PASS roll sat beside it (platform_far/W: roll 1 warped, roll 2 PASS never judged) | — | worked around by hand (reset to the PASS roll — which then also warped on span) | first-exit-wins by roll index prefers a corrected roll over a clean one | B-SWEEPORDER owed: judge every arrival, prefer a measured PASS over any warp exit |
| 19 | the measured-miss round: re-emitted with the warp's own numbers as words ("came back 37% too wide and 14% too tall — do not repeat that") on the frame draft | in flight | — | the reroll research's lever: words fix scale where seeds break it | — |
| 20 | the words round measured | 10 min | **negative**: span 51% and 53% wide — worse than before; the miss-sentence lever fixes material scale, not composition | the zoom-in bias is compositional; words do not reach it (amends the reroll-research law's scope) | — |
| 21 | the true-shape recreate: Image 1 = the warp's own geometry-exact output, ask inverted (keep architecture exactly, redraw objects true) | ~25 min | **platform/E CONVERGED**: span 577 px (−1.4%), sx 1.014, live disc 66x62 = 0.939 round — the first deep wall to hold framing all day, and the disc healed. platform_far/W recomposed again (26% wide; its door invites the zoom); reset onto its better roll (11% wide): live disc 0.754 via the milder warp — its best available state | a full-frame reference leaves nothing to zoom into, and the painter re-normalizes shapes unless ordered to copy — both proven behaviours, composed | L-TRUESHAPE; far-W's next lever is another true-shape pass from its NEW milder warp output, or B-SWEEPORDER first |
| 22 | [Kabe] "the smear should not need to be done to that extent after the image generation. Before is fine for reference but after it hopefully is geometrically CLOSE"; and "platform far W is still kind of a mess" (42% of its frame was warp fill) | ~50 min | **the warp is a reference-maker, not a finisher**: a CLOSE budget in `_exit_warp` (every stretch within 0.93–1.075, revealed ≤ 8%) — beyond it the warp only teaches; `mesh_warp` now saves `revealed.png` (the exact smear mask, stashed at the plane path's own site after a mis-splice broke the file — repaired, parse-before-write re-learned); `deep-draft --true_shape` cuts the mask (+32 px grow) to plain ground with the declared geometry ruled through; the ask names the cut margins as damage to complete naturally. Round 5 ordered on both walls | the true-shape round had copied the warp's own edge smear as if it were material — obedience preserves what the reference shows, so the reference must not show the smear | L-CLOSE |
| 23 | round 6 (blur-in-place, full-frame, corners pinned in words) measured | ~30 min | E's discs healed from a squeezed reference (0.75 in Image 1 → 0.95 painted — the object-true clause works even against the reference); far-W copied its oval (0.65); **both camera PASS, both HELD (unfitted-horizon), both box-expanded again** (E ~33%, far-W 59% revealed on the warp's corner-read) — the CLOSE gate held both on grid | **six rounds falsify every reference treatment for the box**: uniform draft, corrected-previous, frame lines, measured-miss words, cut margins, blur-in-place — the painter's deep-view box recomposition is a constant (~25-35%), while material scale (camera) and object shapes are reliably teachable | R-DEEPBOX: the residual is architectural, not promptable — either the warp finishes deep views (accepting ~0.8 discs + healable edges), or deep views stop being paintings: the renderer composes them from the promoted CLOSE art, shape-true by construction (the through-view machinery already does this for door views). Kabe's call |
| 24 | [Kabe] the chop: the close-up chopped by plane and re-projected (face at ONE uniform scale, sweeps on their true recession, near ring edge-blur filled, "enhance so the fill blur matches") + [Kabe] the wireframe on the reference ("dark lines outlining the corners... it should maintain that in the final image") | rounds 7-7b | drafts: disc 0.957, true fill 14.3%; round-7b paintings the campaign's best (discs 0.88-1.00, junctions straight); far-W whose core corners COINCIDED with the drawn lines hit span 679 px (16% wide) - the best box of seven rounds; E, whose ruler-anchored core sat 46 px inside its lines, split outward (822/870) | the core must sit ON the wireframe | corner-anchored scale (round 8) |
| 25 | round 8: the chop core anchored to the declared corners | ~20 min | **the horizon became FITTABLE for the first time in the campaign**: E's hold moved from unfitted-horizon to suspect-painting (returns converge along straight lines - at row 578.3 vs declared 526); E's best roll a73c3062: camera PASS +1.17/-1.40 (best of all eight rounds), span 655 px (12% wide, was 41-49%); far-W 95cdd884 span 667 (14%). No roll inside CLOSE (residuals: box 12-24% wide, storey/convergence low, sy asks 0.76-0.99) | box span across the rounds: ~800-895 → 679 → 655/667 - converging, asymptotically | Kabe's call: bootstrap round 9 (best roll re-chopped as the next core), or an exit from R-DEEPBOX |
| 26 | [Kabe] pre-compensation ("if the output overshoots by 12%, undershoot the reference by about 12%"): the reference built as if the camera stood further back by the wall's own last measured overshoot, one uniform factor | round 9 | **the compensation itself measured negative** — spans grew (925/879 vs round 8's 655): the painter's bias is an ATTRACTOR (it re-zooms any reference toward its preferred composition), not an offset to aim against. **But the round produced the campaign's first outright promotion**: platform/E roll a73c3062 passed the camera (+1.17/−3.22) AND the promotion instrument (returns fitted, ruler true) and promoted on the MEASURED path — no warp, no correction, disc 84x84 = 1.000 live | a self-consistent painting is what the instruments reward; the chop+wireframe+enhance stack delivers them | far-W re-rolled on the standing recipe |
| 27 | round 10 (far-W on the stale-compensated reference) | 15 min | both rolls camera FAIL small (focal -15/-20, eye +9): the 0.832 compensation left active fed the known-dead lever into the reference — my miss, one round spent on it | a lever measured negative must be REMOVED the same hour, not left for the next emission to drink | comp forced 1.0 with the tombstone comment; far-W re-rolled clean (round 11) |
| 28 | rounds 10-11 (clean re-rolls) both camera FAIL small; the loop PARKED far-W — cap spent, its own stop sign honoured | ~40 min | no more rolls bought. The best roll of the campaign for this wall (round 9's a6a27b21: camera PASS −7.9/+2.3, held unfitted-horizon, disc round) was recovered FROM GIT — every retire had been committed — identified positively by sha-match against its own archived reading, restored, re-read by the loop (PASS, same numbers: the instrument is a pure function), and **promoted under the Captain's row-32 tolerance ruling**: declared camera, suspect_perspective flag, NO warp — the painted pixels stand | retired rolls must be archived under round-stamped names (the clobbering cost an hour of git forensics — applied from round 11 on); the ledger's own tolerance route was the door for a camera-PASS/unfittable-horizon wall all along | **12/12. Both deep walls painted, unwarped, discs round. Published.** |
