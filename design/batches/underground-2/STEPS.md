# underground-2 — step log (2026-08-30)

[Kabe]: a new two-room test, genre the Navigator's pick, unique to test genre-specific things; room two "two room squares wide", walked with an up arrow, not a turn; all rooms counted in small room boxes. Genre picked: a 1931 London Underground station, BRUNSWICK HALT — glazed tiling, a dado band ruler, no windows below ground (the first pack to stress the no-window clause everywhere), and a hand-tinted-photograph medium.

| # | step | time | outcome | hang-up / cause | fix (now in the tree) |
|---|---|---|---|---|---|
| 0 | author packs/underground-2: booking hall (1 box) + PLATFORM (2 boxes: cells `platform` + `platform_far`, one name, joined by a full-width `open_edge`); box unit 6.4 m recorded in pack.json | ~1 turn | plan authored | — | — |
| 1 | the long-room law | — | validator refused: "wall_line 19.8 is not the line this facing views (13.4)" — a facing had to view its own edge | the deep facings look ACROSS the open edge to the far cell's wall (two paintings of one wall at 11.2 m and 4.8 m; the up arrow cuts between them) | validate-plan: a facing may view the first RULED line across its own full-width open edges (`throughLine`, 8-hop cap); a narrower mouth still views its edge (the court) |
| 2 | the up arrow | — | built | the page had no walk-forward control (thresholds were click-hunting) | `#chevron-forward` (chrome, bottom-centre, ↑ / ArrowUp): shown when the facing carries a `way_` exit (derive-world's name for an open_edge); dispatches the same go; keyboard.spec pins the new focusable |
| 3 | same-name narration | — | built | "You pass from the platform into the platform." | derive-world: walking within one name reads "You walk on down / back along the platform." (directed by the exit's facing so the two ways differ — the validator refuses shared prose) |
| 4 | derive → validate → bake → emit | ~40 s | 12/12 packets (3 leads), prompts leak-clean; the deep ask reads "the whole 6.4 m of the wall spans about half the picture's width" (11.2 m camera — the long-platform shot) | whereami.spec's surface census was missing every non-manor place (red since cyberpunk, unnoticed in the suite's standing red) | the 8 places enumerated in design/surface-strings.md; spec green |
| 5 | paint + measure, first pass | leads at 2 min, all 12 by 6 min | booking_hall/E promoted (PASS −5.9%), platform/W promoted (warped); **platform_far/W HELD**: "every column band this scaffold declares falls outside the frame" | the crossed open edge registered as the deep facing's CARRIER: projected nearer than the viewed wall it spanned the whole frame, swallowed every column band, and the ask read "there is no wall here at all ... flanked by low coursed-stone piers" (the manor court's own language) — which the painter faithfully painted (dark voids and stone piers beside a floating wall) | `facingCarriers`: an open edge is a carrier only when the facing views its own edge; a deep facing takes the viewed wall's carriers. Both deep asks re-emitted clean (carriers ['door'], two column bands); the two polluted candidates discarded; 2 re-asks sent (the run's first) |
| 5b | (observed in the same pass) | — | one transient "end-of-sweep bake refused: cePromise.__proto__ (node ESM loader)" — the next pass baked clean | unexplained one-off in the loop's bake subprocess | watched; recurs → its own row |
| 5c | two loops baked the shared served tree at once and the fixed `served.building` name made them delete each other's half-encoded work | — | **fixed**: per-process build dirs (`served.building-<pid>`), stale ones swept after 15 min; both swaps land whole trees, last writer wins | my atomic-swap fix assumed one baker | — |
| 6 | the two clean deep paintings held AGAIN on the old reason | 15 min to root | the re-emit reuses deterministic roll ids; the painter delivered NEW paintings at the SAME paths; the reading cache (id + instrument) served the OLD paintings' readings for the new bytes | "a reading is a pure function of its bytes and the instrument" — the id stood in for the bytes and the bytes moved under it | readings and the routing latch carry `candidate_sha256`; a repainted candidate at the same path is measured and routed afresh; the two stale readings deleted, loop restarted |
| 7 | the warp carried platform_far/W and the PROMOTION refused it: door01 and way01 "share 110 px — one hole cannot be two ways through" | 15:51 | **fixed, promoted 15:55 (warped)** | the crossed-edge law lived in `facingCarriers` only; its sibling `openingsForFacing` still wrote the full-width open edge into the deep wall's meta as a threshold | the same law in the threshold block; the JS projection/promotion files joined the instrument digest so a fix there re-routes latched walls by itself |
| 7b | my latch patch crashed the loop (NameError: `reading` out of scope in `_record_exit`) | 15:48 | fixed (parameter, not a free variable) | patched live and written to disk before its syntax parse ran | the causes note (E): parse before write, one gesture for reset-and-restart |
| 8 | [Kabe] "determine what will prevent rerolls — assume we did it bad; better prompting, and better is not more" | 90 min | **answered with an experiment** (design/audit/reroll-research-2026-08-30.md): the 3 far-cell rerolls — seeded rolls FAIL −13/−12/−9 %, unseeded re-rolls with the SAME words PASS +0.4/−3.5/−7.4 — the wrong Image 1 caused them, not the words | one attached image out-argues every sentence (row 40's shields, now scale) | a seed must PASS the instrument before it may teach (styleImageFor gate); the warp is reachable for scale misses (one reading threaded in — its own snap re-read had crashed on shape); a partial emit no longer guts the manifest's skipped entries (the sweep could not see the walls or their retry rolls) |

**Totals.** 12 walls, 15 images (1.25/wall — the three rerolls are the research subject of step 8 and paid for the seed-gate law), 12/12 promoted (8 warped, 4 measured); the up arrow walks the two-box platform both ways. Live: https://5000stadia.github.io/holo-emitter/?world=underground-2

**What this genre bought the engine:** the long-room laws (a facing views the first ruled line across its full-width open edges; a crossed edge is neither carrier nor threshold of the viewed wall), the up-arrow control, same-name narration, the seed-gate law (a picture must pass the instrument before it may teach), one reading threaded into the warp, byte-keyed reading identity, skip-preserving manifests, per-process bakes — and the no-window clause held on all 12 walls.
| 9 | [Kabe] the long-room views don't match in detail ("geometrically sound as a room but not as details"); his mechanism: "a separate image request made proper to the image gen, acknowledging FROM the close up that this is based off that picture but X feet backwards" | ~60 min | **built, two walls re-asked** | two independent asks painted one wall twice (disc grew a face, one lamp became three): nothing carried the shared wall's CONTENT between standpoints, and the register's Image-1 clause ("take no architecture from it") is the opposite of what a deep view needs | `sameWallImageFor` in the one home (`styleImageFor`): a deep facing's Image 1 is the promoted CLOSE painting, raw, with the identity sentence and the camera move in metres; the register and PACKET.md say exactly that; gated on promotion (a seed must pass the instrument). Three of my own slips on the way (a third style call site, an unimported `relative`, template text left speaking derived-seed language) — each caught by reading the emitted packet before ordering paint. A deterministic derived-view assembler (B-DEEPVIEW, capped builder) runs in parallel as the comparison |
