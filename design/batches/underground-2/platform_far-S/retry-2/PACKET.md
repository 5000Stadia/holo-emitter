# platform_far/S — RE-ASK 2 (technique t2, labelled scaffold)

> **Why this wall is being asked again**
>
> draw 1.133x larger: 213.3 px/m at the wall plane, not 188.3

**Image 2 is this wall's edge seed.** `edge-seed-right.png` is the 10 % of `backdrops/source/platform_far-W/row23-a6a27b21.png` that abuts this picture — its left-hand 154 columns, full frame height, cut by `tools/crop-edge-seed.py` (sha256 `aaab029a9e2c` from a painting at `912cf3ec78d0`). The prompt names its role in words: _Image 2 is a reference of exactly what sits at this picture's right edge - the scene continues from it seamlessly._

Seeding here is **opportunistic** — an indoor location: the strip anchors material tone and the wainscot line across the corner (candidate).

There is NO Image 1 in this packet and none is to be found elsewhere — the medium is in the prompt's own words. Attach `scaffold.png` as **Image 1** and `edge-seed-right.png` as **Image 2**, in that
order, then send `prompt.txt` verbatim. Generate 1 images and save them to the
exact paths below — the measurement runs the moment a file appears at one of them.

| roll 1 | `backdrops/source/platform_far-S/row23-d9721d31.png` |

The prompt files are already on disk beside them. Do not rewrite them.

This wall: 213.3 px per metre at the wall plane, no carrier — unbroken cream crackle-glazed tiling above the dado band with dark bottle-green glazed tiles below.
Voice: **platform_vault** (room id); gate anchor **the tiled dado band's top edge above the floor**, 1.20 m.
Register: **g5-noappendix** — the register this ask was composed in (tools/frame-language.mjs, row 43). Every
roll below is attributable to it: the reading of a return joins to this line through the roll id.
The earlier ask for this wall is still at `../` and is not overwritten.
Write only under `backdrops/`. Never `src/`, never `design/`.
