# Packet — study/N, technique t3 (scaffold + style + text variants)

**Generate 4 images. Save each to the exact path in the table.** The measurement runs
the moment a file appears at one of those paths, so a return in the right place under the wrong name
costs a wave.

## Attach, in this order

1. `style-seed-warm.png` — **Image 1**, the style reference. Kabe's approved seed ("Warm",
   `design/approvals.log`, 2026-08-21).
2. `study-N-scaffold.png` — **Image 2**, the annotated layout scaffold.

Then send the prompt text verbatim from the file named in the table.

## The rolls

| roll | prompt to send | save the image to |
|---|---|---|
| 1 | `prompt-VA.txt` | `backdrops/source/study-N/row23-99ac9099.png` |
| 2 | `prompt-VA.txt` | `backdrops/source/study-N/row23-7432e756.png` |
| 3 | `prompt-VB.txt` | `backdrops/source/study-N/row23-980cfda6.png` |
| 4 | `prompt-VB.txt` | `backdrops/source/study-N/row23-5f7ef55f.png` |

**The prompt files are already on disk beside where the image goes** (`backdrops/source/study-N/row23-<id>.prompt.txt`),
written from this packet, so a return needs nothing but the PNG. Do not rewrite them.

## What this wall is

study/N at 188.421 px per metre at the wall plane, drawn at the camera the page holds for this facing. Its plan carrier
is the **fireplace**, and the gate's one voting
ruler is the wainscot chair-rail at 0.95 m above the floor line.

## The fence

Write only under `backdrops/` and `library-src/`. Never `src/`, never `design/`. Nothing here
asks you to judge a result: generate, save to the named paths, and report the paths back.
