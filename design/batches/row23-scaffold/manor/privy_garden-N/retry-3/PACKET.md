# privy_garden/N — RE-ASK 3 (technique t2, labelled scaffold)

> **Why this wall is being asked again**
>
> VETOED BY KABE 2026-08-24, verbatim: 'exterior garden has interior wall outside' - the archetype prompt gave an OPEN garden room interior oak panelling and a chair-rail. The re-ask must describe the privy garden's own fabric: brick/stone garden wall, sky, planting - no interior materials, no wainscot. The promoted art was demoted; the wall is grid again until an outdoor-true candidate passes.

**The prompt does not quote that sentence.** This is an outdoor facing, and the sentence names interior fabric — which is the very thing it is vetoing. Putting those words in the prompt would put them in front of the generator on the one wall they were vetoed from, and `prompt_lint.py` refuses a packet that does. The prompt carries the forward half instead; the reason lives here.

Attach `style-seed-warm.png` as **Image 1** and `scaffold.png` as **Image 2**, in that
order, then send `prompt.txt` verbatim. Generate 2 images and save them to the
exact paths below — the measurement runs the moment a file appears at one of them.

| roll 1 | `backdrops/source/privy_garden-N/row23-b3e9e51b.png` |
| roll 2 | `backdrops/source/privy_garden-N/row23-86ae7915.png` |

The prompt files are already on disk beside them. Do not rewrite them.

This wall: 198.9 px per metre at the wall plane, no carrier — unbroken weathered brickwork on its stone plinth.
Voice: **outdoors_walled** (room id); gate anchor **the stone string-course above the ground**, 0.95 m.
The earlier ask for this wall is still at `../` and is not overwritten.
Write only under `backdrops/`. Never `src/`, never `design/`.
