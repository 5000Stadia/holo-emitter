# great_hall/W — RE-ASK 5 (technique t2, labelled scaffold)

> **Why this wall is being asked again**
>
> camera PASS; held for the promotion instrument [promotion-refused]: promote refused: great_hall/W: the plan rules 2 way(s) through this wall and the painting shows 0 — a doorway the world walks through with no hole in the picture is not promotable, because a player wo

Attach `style-seed-warm.png` as **Image 1** and `scaffold.png` as **Image 2**, in that
order, then send `prompt.txt` verbatim. Generate 1 images and save them to the
exact paths below — the measurement runs the moment a file appears at one of them.

| roll 1 | `backdrops/source/great_hall-W/row23-1b91cd6b.png` |

The prompt files are already on disk beside them. Do not rewrite them.

This wall: 93.5 px per metre at the wall plane, door + door.
Voice: **hall_state** (room id); gate anchor **the wainscot chair-rail above the floor**, 0.95 m.
The earlier ask for this wall is still at `../` and is not overwritten.
Write only under `backdrops/`. Never `src/`, never `design/`.
