# Packet — great_hall/S, arm g5 (CLEAN-REGISTER)

Kabe's order: the room and its materials first, then this wall's carriers, then the picture in words with the layout image carrying the lines, then the medium, then nothing else - with the coordinate block kept as an appendix at the end

**Generate 1 image. Save it to the exact path in the table.** The measurement runs the
moment a file appears at one of those paths, so a return in the right place under the wrong name
costs a roll.

## Attach, in this order

1. `scaffold.png` — **Image 1** — the annotated layout scaffold

**There is no style image, and that is not an omission.** [HUMAN, 2026-08-24] "So why do we give it the reference image of the study? I think it biases it too much." Image 1 is never a wall from another room, this room has no agreeing majority wall to lend, and the medium is in the prompt's own words. Attach the one image above as **Image 1**.

Then send the prompt text verbatim from `prompt.txt`.

## The rolls

| roll | save the image to |
|---|---|
| 1 | `backdrops/source/great_hall-S/register-c8f7156e.png` |

**The prompt file is already on disk beside where each image goes** (`backdrops/source/great_hall-S/register-c8f7156e.prompt.txt`), written
from this packet, so a return needs nothing but the PNG. Do not rewrite them.

## What this wall is

great_hall/S, generation 1. 112.6 px per metre at the wall
plane; window + window + door + window + window.
Voice **hall_state**; gate anchor **the wainscot chair-rail above the floor**, 0.95 m.

## The fence

Write only under `backdrops/`. Never `src/`, never `design/`. Nothing here asks you to judge a
result: generate, save to the named paths, and report the paths back.
