# wall/oak-wainscot-limewash — flat material swatch

**Converted from the harvest lane.** no promoted facing supplies the lattice this material's own consumers demand (476 px/m): the source supplies 161/161 px/m against a 476 px/m lattice — 2.96x short; the source supplies 159/159 px/m against a 476 px/m lattice — 3.00x short

Send `prompt.txt` verbatim. **Attach no image** — this ask carries no
reference on purpose: there is no geometry to show, and the style seed is a
LIGHTING reference which is the one thing a neutral sample must not inherit.

Generate 2 images and save them to the exact paths below.

| roll 1 | `backdrops/textures/source/wall-oak-wainscot-limewash/row36-91add664.png` |
| roll 2 | `backdrops/textures/source/wall-oak-wainscot-limewash/row36-51cfd593.png` |

Scale contract (periodic): 4 × wainscot bay at 0.8 m = 3.2 m across, 480 px/m.

Write only under `backdrops/`. Never `src/`, never `design/`.
